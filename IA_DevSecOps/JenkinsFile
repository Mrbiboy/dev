pipeline {
    agent any

    environment {
        PYTHON_VENV = "/var/jenkins_home/venv"
    }

    stages {
        stage('User Input') {
            steps {
                script {
                    env.GITHUB_REPO_URL = input(
                        message: '🔗 Entrez le lien du dépôt GitHub à scanner :',
                        ok: 'Lancer le Scan',
                        parameters: [
                            string(name: 'GITHUB_REPO_URL', defaultValue: 'https://github.com/Mrbiboy/dev.git', description: 'Lien du dépôt GitHub')
                        ]
                    )
                }
            }
        }

        stage('Checkout') {
            steps {
                echo "📥 Clonage du dépôt GitHub: ${env.GITHUB_REPO_URL}..."
                sh '''
                    rm -rf IA_DevSecOps || true
                    git clone ${GITHUB_REPO_URL} IA_DevSecOps
                '''

                echo "📂 Affichage du contenu du dépôt après clonage"
                sh 'ls -R IA_DevSecOps'
            }
        }

        stage('Run Extraction Files') {
            steps {
                echo "🔍 Extraction des fichiers..."
                sh '''
                    . $PYTHON_VENV/bin/activate
                    cd IA_DevSecOps
                    python3 extraction.py
                '''
            }
        }

        stage('Verify Configuration Files') {
            steps {
                echo "📂 Vérification des fichiers dans configurations/"
                sh '''
                    if [ -d "IA_DevSecOps/configurations" ]; then
                        ls -l IA_DevSecOps/configurations/
                    else
                        echo "❌ Le dossier configurations/ n'existe pas !"
                        exit 1
                    fi
                '''
            }
        }

        stage('Fix Permissions') {
            steps {
                echo "🔑 Correction des permissions"
                sh 'chmod -R 777 IA_DevSecOps/configurations/'
            }
        }

        stage('Run Terraform Analysis') {
            steps {
                echo "🔍 Analyse Terraform..."
                sh '''
                    . $PYTHON_VENV/bin/activate
                    cd IA_DevSecOps
                    python3 analyze_terraform.py
                '''
            }
        }

        stage('Run Dockerfile Analysis') {
            steps {
                echo "🔍 Analyse Dockerfile..."
                sh '''
                    . $PYTHON_VENV/bin/activate
                    cd IA_DevSecOps
                    python3 analyze_dockerfile.py
                '''
            }
        }

        stage('Run YAML Analysis') {
            steps {
                echo "🔍 Analyse YAML..."
                sh '''
                    . $PYTHON_VENV/bin/activate
                    cd IA_DevSecOps
                    python3 analyze_k8s.py
                '''
            }
        }

        stage('Check JSON Reports') {
            steps {
                echo "📄 Vérification du contenu des fichiers JSON"
                sh '''
                    ls -l IA_DevSecOps/*.json || echo "❌ Aucun fichier JSON valide trouvé !"
                '''
            }
        }

        stage('Archive Reports') {
            steps {
                echo "📦 Archivage des rapports JSON..."
                archiveArtifacts artifacts: 'IA_DevSecOps/*.json', fingerprint: true
            }
        }
    }

    post {
        success {
            echo "✅ Pipeline terminé avec succès !"
        }
        failure {
            echo "❌ Erreur dans le pipeline. Vérifiez les logs."
        }
    }
}
